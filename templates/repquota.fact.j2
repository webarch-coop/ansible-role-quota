#!/usr/bin/env bash

# {{ ansible_managed }}

set -euo pipefail

REPQUOTA="{{ quota_repquota }}"

if [[ -f "{{ quota_dir }}/aquota.user" ]]; then
  if [[ -f "${REPQUOTA}" ]]; then
    repquota "{{ quota_dir }}" -O csv | jc -m --csv 
  fi
fi
