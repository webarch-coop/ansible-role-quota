#!/usr/bin/env bash

# {{ ansible_managed }}

set -euo pipefail

jc="{{ quota_jc }}"
repquota="{{ quota_repquota }}"

if [[ -f "${jc}" && -f "${repquota}" && -f "{{ quota_dir }}/aquota.user" ]]; then
  ${repquota} "{{ quota_dir }}" -O csv | ${jc} -m --csv 
else
  jo state=absent
fi
