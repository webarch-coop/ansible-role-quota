#!/usr/bin/env bash

# {{ ansible_managed }}

set -euo pipefail

TUNE2FS="{{ quota_tune2fs }}"
JC="{{ quota_jc }}"

if [[ -f "${TUNE2FS}" && -f "${JC}" ]]; then
  ${TUNE2FS} -l /dev/xvda4 | tail -n +2 | ${JC} --kv
else
  jo state=absent
fi
